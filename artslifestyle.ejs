<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arts & Lifestyle</title>
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" 
     rel="stylesheet">
    <link rel="stylesheet" href="/Styles/book.css">
  <h2 class="mb-4 text-center">📚 Arts & Lifestyle Books</h2>
</head>
<body style="background-color:#ffe5ec">
    <div class="container mt-4">
  
    <form action="/artslifestyle" method="get" class="mb-4">
        <div class="input-group">
            <input 
                type="text" 
                name="search" 
                class="form-control"
                placeholder="Search book by name..."
                value="<%= search || '' %>"
            >
            <button class="btn btn-primary" type="submit">Search</button>
        </div>
    </form>
     <div class="text-center mb-3">
    <button id="toggleViewBtn" class="btn btn-primary btn-sm">
        Change view
    </button>
</div>

 <div class="container-fluid mt-4">
     <div id="bookContainer" class="row">
        <% if (artslifestyle && artslifestyle.length > 0) { %>
            <% artslifestyle.forEach(item => { %>
              <div class="book-item col-md-4 mb-4">
    <div class="card shadow-sm h-100">
        <div class="card-body d-flex align-items-start">
            <img src="<%= item.CoverImage %>" 
                 alt="<%= item.BookName %>" 
                 class="img-fluid rounded shadow-sm"
                 style="height:180px; width:120px; object-fit:cover; flex-shrink: 0; margin-right:15px;">

            <div class="flex-grow-1" style="min-width: 0;"> 
                <div class="d-flex justify-content-between align-items-start">
                    <div class="me-2" style="word-wrap: break-word; overflow: hidden;">
                        <span class="text-muted d-block mb-1" style="font-size: 0.7rem; font-weight: bold; text-transform: uppercase;">Book Name</span>
                        <h6 class="mb-2" style="font-size: 0.95rem; line-height: 1.2;">
                            <%= item.BookName %>
                        </h6>
                    </div>

                    <button 
                        class="btn btn-warning btn-sm pin-btn p-1" 
                       data-book-name="<%= item.BookName %>"
                          data-book-link="<%= item.BookLink %>"
                        title="Pin this book"
                        style="line-height: 1; border-radius: 6px; flex-shrink: 0;"
                    >
                        📌Pin
                    </button>
                </div>

                <% const rating = userRatings && userRatings[item.BookName] ? userRatings[item.BookName] : 0; %>
                <div class="rating mt-2" data-book="<%= item.BookName %>">
                    <span data-value="1" class="<%= rating >= 1 ? 'active' : '' %>">★</span>
                    <span data-value="2" class="<%= rating >= 2 ? 'active' : '' %>">★</span>
                    <span data-value="3" class="<%= rating >= 3 ? 'active' : '' %>">★</span>
                    <span data-value="4" class="<%= rating >= 4 ? 'active' : '' %>">★</span>
                    <span data-value="5" class="<%= rating >= 5 ? 'active' : '' %>">★</span>
                </div>
            </div>
        </div>

        <div class="card-footer bg-white border-top-0 text-center pb-3">
            <a href="<%= item.BookLink %>" class="btn btn-sm btn-primary w-75">Read book</a>
        </div>
    </div>
</div>

            <% }) %>
        <% } else { %>
            <p class="text-center">No Arts & Lifestyle books found</p>
        <% } %>
    </div>
</div>
</div>
<a href="/library"><img src="/images/BackButton.gif" alt="BackButton" height="100pt"></a>
<script src="/Scrpit/book.js"></script>

</body>
</html>